const  Product = require("../models/product");




const addProduct = async =(req,res)=>{
    try {
        
    } catch (error) {
        console.log(error);
        
    }
}

module.exports ={
    addProduct
}